<?php
/**
 * MatchRooz — پیکربندی سرویس
 * ---------------------------------------------------------------
 * برای اتصال به دیتای واقعی، «provider» را تغییر دهید و کلید API
 * خود را وارد کنید. بدون هیچ تنظیمی، حالت «demo» فعال است و سایت
 * با داده‌های شبیه‌سازی‌شده‌ی زنده کار می‌کند.
 */

return [

    /*
     |------------------------------------------------------------
     | منطقه زمانی سایت
     |------------------------------------------------------------
     */
    'timezone' => 'Asia/Tehran',

    /*
     |------------------------------------------------------------
     | منبع داده
     |------------------------------------------------------------
     | demo         : داده‌ی شبیه‌سازی‌شده (بدون نیاز به کلید)
     | apifootball  : API-Football  (api-football.com | RapidAPI)
     | thesportsdb  : TheSportsDB   (thesportsdb.com)
     */
    'provider' => 'demo',

    /*
     |------------------------------------------------------------
     | API-Football
     |------------------------------------------------------------
     | ۱) ثبت‌نام در https://dashboard.api-football.com
     | ۲) کلید را در «key» بگذارید.
     | اگر از RapidAPI استفاده می‌کنید host را روی
     | api-football-v1.p.rapidapi.com بگذارید.
     */
    'apifootball' => [
        'key'      => '',
        'host'     => 'v3.football.api-sports.io',
        'timezone' => 'Asia/Tehran',
    ],

    /*
     |------------------------------------------------------------
     | TheSportsDB  (نسخه رایگان کلید 3 است)
     |------------------------------------------------------------
     */
    'thesportsdb' => [
        'key' => '3',
    ],

    /*
     |------------------------------------------------------------
     | ترجمه‌ی نام تیم‌ها و لیگ‌ها به فارسی
     |------------------------------------------------------------
     | سرویس‌های داده نام‌ها را انگلیسی می‌دهند. با روشن بودن این گزینه،
     | نام‌های شناخته‌شده به فارسی نمایش داده می‌شوند (بیش از ۳۳۰ باشگاه،
     | ۷۰ لیگ و ۹۰ کشور). نام‌های ناشناس دست‌نخورده می‌مانند.
     |
     | برای افزودن یا تغییر یک نام، فایل api/lib/data/teams.php را
     | ویرایش کنید.
     */
    'localize' => true,

    /*
     |------------------------------------------------------------
     | لوگوی تیم‌ها
     |------------------------------------------------------------
     | mode:
     |   auto     : لوگوی سرویس داده، در نبودش فایل محلی (پیش‌فرض)
     |   provider : فقط لوگوی سرویس داده
     |   local    : فقط فایل‌های خودتان
     |   off      : بدون لوگو (فقط نشان رنگی تولیدشده)
     |
     | اگر لوگوی رسمی در دسترس نباشد، سایت خودش یک «سپر» با رنگ‌های
     | واقعی همان باشگاه و حروف اولش می‌کشد؛ پس هیچ‌وقت جای خالی
     | دیده نمی‌شود.
     |
     | برای استفاده از فایل‌های خودتان: local را true کنید و تصاویر را
     | با نام انگلیسیِ خط‌تیره‌دار در پوشه‌ی assets/img/teams بگذارید،
     | مثلاً real-madrid.png و persepolis.png
     */
    'logos' => [
        'mode'          => 'auto',
        'local'         => false,
        'local_pattern' => 'assets/img/teams/{slug}.png',
    ],

    /*
     |------------------------------------------------------------
     | کش (بر حسب ثانیه)
     |------------------------------------------------------------
     | برای کاهش مصرف سهمیه‌ی API. بازی‌های زنده کوتاه‌تر کش می‌شوند.
     */
    'cache' => [
        'enabled'   => true,
        'dir'       => __DIR__ . '/cache',
        'live'      => 25,    // روزی که بازی زنده دارد
        'today'     => 60,    // امروز بدون بازی زنده
        'other'     => 900,   // روزهای دیگر
        'match'     => 25,    // جزئیات یک بازی زنده
        'standings' => 3600,  // جدول رده‌بندی
        'leagues'   => 86400, // فهرست لیگ‌ها
    ],

    /*
     |------------------------------------------------------------
     | لیگ‌های منتخب (ترتیب نمایش در صفحه)
     |------------------------------------------------------------
     | شناسه‌ها مربوط به API-Football هستند.
     */
    'featured_leagues' => [2, 3, 39, 140, 135, 78, 61, 88, 94, 203, 290],

    /*
     |------------------------------------------------------------
     | فصل جاری برای جدول رده‌بندی
     |------------------------------------------------------------
     */
    'season' => (int) date('n') >= 7 ? (int) date('Y') : (int) date('Y') - 1,

    /*
     |------------------------------------------------------------
     | دامنه‌های مجاز برای CORS (خالی = فقط همین دامنه)
     |------------------------------------------------------------
     */
    'cors_origins' => [],

    /*
     |------------------------------------------------------------
     | حالت اشکال‌زدایی (در سرور واقعی false بماند)
     |------------------------------------------------------------
     */
    'debug' => false,
];
